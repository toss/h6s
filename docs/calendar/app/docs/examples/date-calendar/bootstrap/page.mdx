import { Sandpack } from '@/components/Sandpack';

# Bootstrap 5

DateCalendar built with @h6s/calendar and Bootstrap 5 components.

## Interactive Example

Try editing the code below to customize the DateCalendar component:

<Sandpack
  files={[
    {
      path: "app/docs/examples/date-calendar/bootstrap/App.tsx",
      target: "/App.tsx",
    },
    {
      path: "app/docs/examples/date-calendar/bootstrap/DateCalendar.tsx",
      target: "/DateCalendar.tsx",
    },
  ]}
  providerOptions={{
    externalResources: [
      "https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css",
    ],
  }}
  previewOptions={{
    style: {
      height: 580,
    }
  }}
/>

## Features

- Single date selection
- Month/year navigation
- Today shortcut
- Uses Bootstrap 5 utility classes
- Accessible with proper ARIA labels
