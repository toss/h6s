import { Sandpack } from '@/components/Sandpack';
import { loadExample } from '@/components/Sandpack/loadExample';

export const files = loadExample('app/docs/examples/date-calendar/bootstrap', [
  'App.tsx',
  'DateCalendar.tsx',
  'public/index.html',
]);

# Bootstrap 5

DateCalendar built with @h6s/calendar and Bootstrap 5 components.

## Interactive Example

Try editing the code below to customize the DateCalendar component:

<Sandpack
  files={files}
  providerOptions={{
    externalResources: [
      "https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css",
    ],
  }}
  previewOptions={{
    style: {
      height: 580,
    }
  }}
/>

## Features

- Single date selection
- Month/year navigation
- Today shortcut
- Uses Bootstrap 5 utility classes
- Accessible with proper ARIA labels
