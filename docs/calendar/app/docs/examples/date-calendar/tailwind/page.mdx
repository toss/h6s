import { Sandpack } from '@/components/Sandpack';
import { loadExample } from '@/components/Sandpack/loadExample';

export const files = loadExample('app/docs/examples/date-calendar/tailwind/basic', [
  'App.tsx',
  'DateCalendar.tsx',
  'public/index.html',
]);

# Tailwind CSS

DateCalendar built with @h6s/calendar and Tailwind CSS.

## Interactive Example

Try editing the code below to customize the DateCalendar component:

<Sandpack
  files={files}
  providerOptions={{
    externalResources: ["https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"],
  }}
  previewOptions={{
    style: {
      height: 580,
    }
  }}
/>

## Features

- Single date selection
- Month/year navigation
- Today button
- Fully styled with Tailwind CSS
- Dark mode support with `dark:` variants

