import { Sandpack } from '@/components/Sandpack';
import { loadExample } from '@/components/Sandpack/loadExample';

export const files = loadExample('app/docs/examples/date-calendar/vanilla/basic', [
  'App.tsx',
  'App.css',
  'DateCalendar.tsx',
  'DateCalendar.css',
]);

# Vanilla CSS

DateCalendar built with @h6s/calendar and plain CSS.

## Interactive Example

Try editing the code below to customize the DateCalendar component:

<Sandpack
  files={files}
  previewOptions={{
    style: {
      height: 580,
    }
  }}
/>

## Features

- Single date selection
- Month/year navigation
- Today button
- Component-scoped CSS classes
- Dark mode support with `@media (prefers-color-scheme: dark)`

