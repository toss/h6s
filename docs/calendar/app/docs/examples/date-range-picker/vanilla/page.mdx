import { Sandpack } from "@/components/Sandpack";
import { loadExample } from "@/components/Sandpack/loadExample";

export const filesPicker = loadExample('app/docs/examples/date-range-picker/vanilla/basic', [
  'App.tsx',
  'App.css',
  'DateRangePicker.tsx',
  'DateRangePicker.css',
]);

export const filesCalendarBasic = loadExample('app/docs/examples/date-range-picker/vanilla/calendar-basic', [
  'App.tsx',
  'App.css',
  'DateRangeCalendar.tsx',
  'DateRangeCalendar.css',
]);

export const filesCalendarDual = loadExample('app/docs/examples/date-range-picker/vanilla/calendar-dual', [
  'App.tsx',
  'App.css',
  'DateRangeCalendarDual.tsx',
  'DateRangeCalendarDual.css',
]);

# Vanilla CSS

## Basic

Date range picker with dual calendar popover built using plain CSS and BEM-style conventions.

<Sandpack
  files={filesPicker}
  dependencies={{
    "@radix-ui/react-popover": "^1.1.2",
  }}
  height={470}
/>

## Calendar Only

DateRangeCalendar with single calendar view.

<Sandpack
  files={filesCalendarBasic}
  height={480}
/>

## Dual Calendar

For better UX when selecting date ranges, you can display two calendars side by side showing consecutive months.

<Sandpack
  files={filesCalendarDual}
  height={480}
/>

## Features

- Date range picker with field trigger and popover
- Calendar-only components for inline use (basic and dual layouts)
- Field-style trigger with placeholder and clear action
- Floating popover built with `@radix-ui/react-popover`
- Dual calendar layout showing consecutive months
- Date range selection with visual highlighting
- Component-scoped CSS classes
- Dark mode support with `@media (prefers-color-scheme: dark)`
