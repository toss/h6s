import { Sandpack } from "@/components/Sandpack";
import { loadExample } from "@/components/Sandpack/loadExample";

export const filesPicker = loadExample('app/docs/examples/date-picker/tailwind/basic', [
  'App.tsx',
  'DatePicker.tsx',
  'public/index.html',
]);

export const filesCalendar = loadExample('app/docs/examples/date-picker/tailwind/calendar', [
  'App.tsx',
  'DateCalendar.tsx',
  'public/index.html',
]);

# Tailwind CSS

## Basic

Input field with calendar popover for single date selection styled with Tailwind CSS utility classes.

<Sandpack
  files={filesPicker}
  dependencies={{
    "@radix-ui/react-popover": "^1.1.2",
  }}
  providerOptions={{
    externalResources: ["https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"],
  }}
  height={420}
/>

## Calendar Only

Standalone calendar component for inline date selection.

<Sandpack
  files={filesCalendar}
  providerOptions={{
    externalResources: ["https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"],
  }}
  height={400}
/>
