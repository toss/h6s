import { CodeFromFile } from "@/components/CodeFromFile";
import { DemoWithCode } from "@/components/DemoWithCode";
import { DatePicker } from "./DatePicker";

# Vanilla CSS

Single date picker with a field trigger and floating calendar popover built using plain CSS.

<DemoWithCode
  demo={<DatePicker />}
  code={
    <>
      <CodeFromFile
        file="app/docs/examples/date-picker/vanilla/DatePicker.tsx"
        lang="tsx"
        filename="DatePicker.tsx"
      />
      <CodeFromFile
        file="app/docs/examples/date-picker/vanilla/DatePicker.css"
        lang="css"
        filename="DatePicker.css"
      />
    </>
  }
/>



## Features

- Radix Popover gives accessible overlay behavior (focus trap, escape to close).
- Calendar generated by `@h6s/calendar` with month navigation and today highlight.
- Clear and Today actions included in the popover.
- Styling uses vanilla CSS with light/dark support via `prefers-color-scheme`.
- Popover width adjusts to viewport and allows horizontal scrolling when space is limited.
