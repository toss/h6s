import { Sandpack } from '@/components/Sandpack';
import { loadExample } from '@/components/Sandpack/loadExample';

export const filesBasic = loadExample('app/docs/examples/date-picker/bootstrap/basic', [
  'App.tsx',
  'DatePicker.tsx',
  'useBootstrapTheme.ts',
  'public/index.html',
]);

export const filesCalendar = loadExample('app/docs/examples/date-picker/bootstrap/calendar', [
  'App.tsx',
  'DateCalendar.tsx',
  'useBootstrapTheme.ts',
  'public/index.html',
]);

# Bootstrap 5

## Basic

Input field with calendar popover for single date selection styled with Bootstrap 5 utility classes.

<Sandpack
  files={filesBasic}
  dependencies={{
    "@radix-ui/react-popover": "^1.1.2",
  }}
  providerOptions={{
    externalResources: [
      "https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css",
    ],
  }}
  height={460}
/>

## Calendar Only

Standalone calendar component for inline date selection.

<Sandpack
  files={filesCalendar}
  providerOptions={{
    externalResources: [
      "https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css",
    ],
  }}
  height={420}
/>

