import { Sandpack } from '@/components/Sandpack';
import { loadExample } from '@/components/Sandpack/loadExample';

export const filesBasic = loadExample('app/docs/examples/date-range-calendar/vanilla/basic', [
  'App.tsx',
  'App.css',
  'DateRangeCalendar.tsx',
  'DateRangeCalendar.css',
]);

export const filesDual = loadExample('app/docs/examples/date-range-calendar/vanilla/dual', [
  'App.tsx',
  'App.css',
  'DateRangeCalendarDual.tsx',
  'DateRangeCalendarDual.css',
]);

# Vanilla CSS

DateRangeCalendar built with @h6s/calendar and plain CSS.

## Basic

DateRangeCalendar with single calendar view.

<Sandpack
  files={filesBasic}
  previewOptions={{
    style: {
      height: 650,
    }
  }}
/>

## Dual Calendar

For better UX when selecting date ranges, you can display two calendars side by side showing consecutive months.

<Sandpack
  files={filesDual}
  previewOptions={{
    style: {
      height: 650,
    }
  }}
/>

## Features

- Date range selection (start and end dates)
- Visual range highlighting between selected dates
- Hover preview showing potential range
- Clear button to reset selection
- Month/year navigation
- Component-scoped CSS classes
- Dark mode support with `@media (prefers-color-scheme: dark)`

