import { Sandpack } from '@/components/Sandpack';
import { loadExample } from '@/components/Sandpack/loadExample';

export const filesBasic = loadExample('app/docs/examples/date-range-calendar/tailwind/basic', [
  'App.tsx',
  'DateRangeCalendar.tsx',
  'public/index.html',
]);

export const filesDual = loadExample('app/docs/examples/date-range-calendar/tailwind/dual', [
  'App.tsx',
  'DateRangeCalendarDual.tsx',
  'public/index.html',
]);

# Tailwind CSS

DateRangeCalendar built with @h6s/calendar and Tailwind CSS.

## Basic

DateRangeCalendar with single calendar view.

<Sandpack
  files={filesBasic}
  providerOptions={{
    externalResources: ["https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"],
  }}
  previewOptions={{
    style: {
      height: 650,
    }
  }}
/>

## Dual Calendar

For better UX when selecting date ranges, you can display two calendars side by side showing consecutive months.

<Sandpack
  files={filesDual}
  providerOptions={{
    externalResources: ["https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"],
  }}
  previewOptions={{
    style: {
      height: 650,
    }
  }}
/>

## Features

- Date range selection (start and end dates)
- Visual range highlighting between selected dates
- Hover preview showing potential range
- Clear button to reset selection
- Month/year navigation
- Tailwind CSS utility classes
- Dark mode support with `dark:` variants

