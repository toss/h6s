import { DateCalendar } from './DateCalendar';
import { CodeFromFile } from '@/components/CodeFromFile';
import { DemoWithCode } from '@/components/DemoWithCode';
import { BootstrapPreview } from '@/components/BootstrapPreview';

# Bootstrap 5

DatePicker built with @h6s/calendar and Bootstrap 5 components.

## Basic

<DemoWithCode
  demo={
    <BootstrapPreview>
      <DateCalendar />
    </BootstrapPreview>
  }
  code={
    <CodeFromFile
      file="app/examples/date-calendar/bootstrap/DateCalendar.tsx"
      lang="tsx"
      filename="DateCalendar.tsx"
    />
  }
/>

## Features

- Single date selection
- Month/year navigation
- Today shortcut
- Uses Bootstrap 5 utility classes
- Accessible with proper ARIA labels
