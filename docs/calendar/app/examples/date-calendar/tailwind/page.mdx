import { DatePicker } from './DatePicker';
import { CodeFromFile } from '@/components/CodeFromFile';
import { DemoWithCode } from '@/components/DemoWithCode';

# Tailwind CSS

DatePicker built with @h6s/calendar and Tailwind CSS.

## Basic

<DemoWithCode
  demo={<DatePicker />}
  code={
    <CodeFromFile
      file="app/examples/date-calendar/tailwind/DatePicker.tsx"
      lang="tsx"
      filename="DatePicker.tsx"
    />
  }
/>

## Features

- Single date selection
- Month/year navigation
- Today button
- Fully styled with Tailwind CSS
- Dark mode support with `dark:` variants

## Key Implementation Details

### Dark Mode Support

The picker adapts to dark mode using Tailwind's `dark:` variant:

- **Container**: `dark:bg-gray-900` background and `dark:border-gray-700` border
- **Typography**: `dark:text-gray-100` for headings and values
- **Controls**: Buttons use `dark:hover:bg-gray-800` and `dark:focus:ring-blue-400`
- **Inactive dates**: `dark:text-gray-600` for days outside the current month

The component respects the system or site-wide dark mode setting automatically.

## Customization

You can customize this picker:

- **Colors**: Change `blue-500` to your brand color
- **Size**: Adjust `w-10 h-10` for larger/smaller date cells
- **Layout**: Tweak padding and spacing utilities on the container sections
- **Format**: Change date format strings in `format()` calls
- **Week Start**: Pass `defaultWeekStart` option to `useCalendar`

## See Also

- [Date Range Picker (Vanilla)](/examples/date-range-picker/vanilla)
- [Bootstrap 5 Example](/examples/date-picker/bootstrap)
- [Vanilla CSS Example](/examples/date-picker/vanilla)
- [API Reference](/api-reference)
